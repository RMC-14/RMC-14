using Robust.Client.AutoGenerated;
using Robust.Client.Graphics;
using Robust.Client.UserInterface.CustomControls;
using Robust.Client.UserInterface.XAML;

namespace Content.Client._RMC14.Sentry.Laptop;

[GenerateTypedNameReferences]
public sealed partial class SentryLaptopWindow : DefaultWindow
{
    public SentryLaptopWindow()
    {
        RobustXamlLoader.Load(this);

        if (CameraViewport != null)
        {
            CameraViewport.ViewportSize = new Vector2i(300, 300);
            CameraViewport.Eye = new FixedEye();
        }
    }
}
