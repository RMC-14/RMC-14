using Content.Client._RMC14.Xenonids.UI;
using Content.Shared._RMC14.Xenonids.Watch;
using Robust.Client.AutoGenerated;
using Robust.Client.UserInterface.CustomControls;
using Robust.Client.UserInterface.XAML;
using Robust.Shared.Prototypes;

namespace Content.Client._RMC14.Xenonids.Watch;

[GenerateTypedNameReferences]
public sealed partial class XenoWatchWindow : DefaultWindow
{
    public XenoWatchWindow()
    {
        RobustXamlLoader.Load(this);

        /*
        for (int idx = 0; idx < 2; idx++)
        {
            var control = new XenoTierRow();
            control.SetInfo(5, 2);
            control.AddXeno(2, "Carrier");
            control.AddXeno(2, "Boiler");
            control.AddXeno(2, "Drone");
            control.AddXeno(3, "Crusher");
            //control.TierButton.OnPressed += _ =>
            RowContainer.AddChild(control);
        }
        var controll = new XenoTierRow();
        controll.SetInfo(5, 2);
        controll.AddXeno(2, "Carrier");
        controll.AddXeno(2, "Boiler");
        controll.AddXeno(2, "Drone");
        controll.AddXeno(3, "Crusher");
        controll.AddXeno(3, "Hivelord");
        controll.AddXeno(3, "Burrower");
        RowContainer.AddChild(controll);
*/
    }
}

