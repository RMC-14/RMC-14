using Robust.Client.AutoGenerated;
using Robust.Client.Graphics;
using Robust.Client.UserInterface.Controls;
using Robust.Client.UserInterface.XAML;
using Robust.Shared.Maths;

namespace Content.Client._RMC14.Requisitions;

[GenerateTypedNameReferences]
public sealed partial class RequisitionsBlackMarketHome : PanelContainer
{
    public RequisitionsBlackMarketHome()
    {
        RobustXamlLoader.Load(this);

        PanelOverride = new StyleBoxFlat
        {
            BackgroundColor = Color.FromHex("#17151a"),
            BorderColor = Color.FromHex("#66333b"),
            BorderThickness = new Thickness(1),
        };

        DialoguePanel.PanelOverride = new StyleBoxFlat
        {
            BackgroundColor = Color.FromHex("#17151a"),
            BorderColor = Color.FromHex("#4a2f34"),
            BorderThickness = new Thickness(1),
        };
        DialoguePanel.Visible = false;
        BriefingButton.Visible = false;
        ContactLabel.FontColorOverride = Color.White;
        CategoriesTitle.FontColorOverride = Color.FromHex("#c6a46f");
    }
}
