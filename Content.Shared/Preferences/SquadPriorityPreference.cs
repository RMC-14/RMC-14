namespace Content.Shared.Preferences
{
    /// <summary>
    /// The squad spawn priority preference for a profile. Stored in database!
    /// </summary>
    public enum SquadPriorityPreference
    {
        None = -1,
        Alpha = 0,
        Bravo = 1,
        Charlie = 2,
        Delta = 3,
    }
}
