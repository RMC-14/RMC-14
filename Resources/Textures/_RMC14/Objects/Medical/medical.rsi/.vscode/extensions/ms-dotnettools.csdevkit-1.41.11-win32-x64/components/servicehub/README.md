This directory contains ServiceHub host configuration files
because the ones that are included in the inserted host NPM packages
are inappropriate for use by a xplat extension that uses a dynamically
placed .NET runtime.
